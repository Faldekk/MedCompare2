<UserControl x:Class="DrugCompare.Features.EvidenceAssistant.EvidenceAssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F3F4F6">
        <Grid.Resources>
            <Style x:Key="SectionStyle" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="0"/>
            </Style>
            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="Background" Value="#374151"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#374151"/>
            </Style>
            <Style x:Key="QuietButton" TargetType="Button">
                <Setter Property="Background" Value="#E5E7EB"/>
                <Setter Property="Foreground" Value="#1F2937"/>
                <Setter Property="BorderBrush" Value="#D1D5DB"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,14">
            <TextBlock Text="Evidence Assistant"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#102027"/>

            <TextBlock Text="Lokalne wyszukiwanie źródeł. Opcjonalny Ollama redaguje odpowiedź wyłącznie ze zweryfikowanych źródeł."
                       Foreground="#607D8B"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Style="{StaticResource SectionStyle}"
                Padding="18"
                Margin="0,0,0,14">

            <StackPanel>
                <TextBlock Text="Zapytanie"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"/>

                <TextBox Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                         Background="#FAFAFA"
                         MinHeight="70"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,12"/>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="170"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Margin="0,0,12,0">
                        <TextBlock Text="Produkt / nazwa źródła — opcjonalnie"
                                   Foreground="#607D8B"/>
                        <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}"
                                 Height="32"
                                 VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Margin="0,0,12,0">
                        <TextBlock Text="Sekcja — opcjonalnie"
                                   Foreground="#607D8B"/>
                        <TextBox Text="{Binding SectionNumber, UpdateSourceTrigger=PropertyChanged}"
                                 Height="32"
                                 VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <CheckBox Grid.Column="2"
                              Content="Uwzględnij needs_review"
                              IsChecked="{Binding IncludeNeedsReview}"
                              VerticalAlignment="Bottom"
                              Margin="0,0,0,8"/>
                </Grid>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Szukaj źródeł"
                            Style="{StaticResource QuietButton}"
                            Command="{Binding SearchCommand}"
                            Height="34"
                            Width="130"
                            Margin="0,0,8,0"/>

                    <Button Content="Wyczyść"
                            Style="{StaticResource QuietButton}"
                            Command="{Binding ClearCommand}"
                            Height="34"
                            Width="90"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Style="{StaticResource SectionStyle}"
                Padding="12">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="Źródła"
                               FontSize="15"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding StatusMessage}"
                               Foreground="#6B7280"
                               HorizontalAlignment="Right"/>
                </DockPanel>
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Results}"
                      SelectedItem="{Binding SelectedResult}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      >

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding Id}"
                                        Width="70"/>

                    <DataGridTextColumn Header="Źródło"
                                        Binding="{Binding SourceTitle}"
                                        Width="240"/>

                    <DataGridTextColumn Header="Sekcja"
                                        Binding="{Binding SectionNumber}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Status"
                                        Binding="{Binding ReviewStatus}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Kategoria"
                                        Binding="{Binding ClinicalCategory}"
                                        Width="140"/>

                    <DataGridTextColumn Header="Substancja czynna"
                                        Binding="{Binding ActiveSubstance}"
                                        Width="170"/>

                    <DataGridTextColumn Header="Źródło URL"
                                        Binding="{Binding SourceUrl}"
                                        Width="220"/>

                    <DataGridTextColumn Header="Tekst"
                                        Binding="{Binding ChunkText}"
                                        Width="*"/>
                </DataGrid.Columns>

            </DataGrid>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <WrapPanel Margin="0,0,0,6">
                <TextBox Text="{Binding ReviewerName, UpdateSourceTrigger=PropertyChanged}"
                         Width="180" Height="30" Margin="0,0,8,0"
                         VerticalContentAlignment="Center"
                         ToolTip="Osoba weryfikująca"/>
                <TextBox Text="{Binding ReviewNote, UpdateSourceTrigger=PropertyChanged}"
                         Width="260" Height="30" Margin="0,0,8,0"
                         VerticalContentAlignment="Center"
                         ToolTip="Notatka weryfikacji"/>
                <Button Content="Reviewed" Style="{StaticResource QuietButton}" Command="{Binding ChangeReviewStatusCommand}" CommandParameter="reviewed" Height="30" Margin="0,0,6,0"/>
                <Button Content="Verified" Style="{StaticResource QuietButton}" Command="{Binding ChangeReviewStatusCommand}" CommandParameter="verified" Height="30" Margin="0,0,6,0"/>
                <Button Content="Rejected" Style="{StaticResource QuietButton}" Command="{Binding ChangeReviewStatusCommand}" CommandParameter="rejected" Height="30"/>
                <Button Content="Otwórz źródło" Command="{Binding OpenSelectedSourceCommand}" Height="30" Margin="8,0,0,0"/>
                <Button Content="Zredaguj odpowiedź RAG" Style="{StaticResource QuietButton}" Command="{Binding GenerateRagAnswerCommand}" Height="30" Margin="8,0,0,0"/>
            </WrapPanel>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#607D8B"/>
            <TextBlock Text="Źródła ze statusem needs_review są wykluczone domyślnie. Ich obecność oznacza dane nieweryfikowane."
                       Foreground="#A15C00" FontSize="12" Margin="0,4,0,0"/>
            <TextBox Text="{Binding GeneratedAnswer}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     MinHeight="90"
                     MaxHeight="220"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
