<UserControl x:Class="DrugCompare.Views.DatabaseStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White"
            CornerRadius="12"
            Padding="14"
            BorderBrush="#D6DEE8"
            BorderThickness="1"
            Margin="0,12,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Database Status"
                               FontSize="18"
                               FontWeight="Bold"/>

                    <TextBlock Text="Overview of local SQLite data currently used by the application."
                               FontSize="12"
                               Foreground="#607080"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Reload status"
                        Command="{Binding LoadDatabaseStatusCommand}"
                        Width="140"
                        Height="34"/>
            </Grid>

            <Border Grid.Row="1"
                    Background="#F8FAFC"
                    BorderBrush="#D6DEE8"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="14"
                    Margin="0,0,0,16">
                <TextBlock Text="{Binding DatabaseStatusText}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"/>
            </Border>

            <TextBlock Grid.Row="2"
                       Text="This tab confirms that the application is running on local SQLite data. Full EMA and DDInter datasets are not committed to the repository; they must be imported locally."
                       Foreground="#607080"
                       FontSize="12"
                       TextWrapping="Wrap"/>
        </Grid>
    </Border>
</UserControl>